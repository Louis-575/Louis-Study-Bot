DISCORD_TOKEN=your_discord_bot_token_here

# Guild registration updates commands quickly while testing.
# Use Global when you are ready to publish commands everywhere.
COMMAND_REGISTRATION=Guild

# Optional settings
STUDY_DATA_PATH=Data/study-sessions.json
LOG_PATH=logs/study-bot-[year]-[month]-[day].log
LOG_LEVEL=Info

# Optional: if the project is mounted from a git repo, pull updates when the container starts.
AUTO_UPDATE=false
